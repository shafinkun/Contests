#include<bits/stdc++.h>
using namespace std;
void arr();
int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        int max=1;
        int n,time;
        cin>>n>>time;
        bool check=true;
        arr(n);
        // int n,time;
        // cin>>n>>time;
        // int vid[n];
        // for(int i=0;i<n;i++)
        // {
        //     cin>>vid[i];
        // }
        // int ent[n];
        // for(int i=0;i<n;i++)
        // {
        //     cin>>ent[i];
        // }
        for(int i=0;i<n;i++)
        {
           // cout<<a[i]<<"\t";
        }
        if(check){
            
        }
        
    }
    return 0;
}
void arr(int b){
    int a[b];
    // int b[n];
    for(int i=0;i<b;i++)
    {
        cin>>a[i];
    // }
    // for(int i=0;i<n;i++)
    // {
        // cin>>b[i];
    }
}